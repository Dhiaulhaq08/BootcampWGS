<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KyZXEJR1hPntw6z2puaG3M7ML4EGRXn0E93v68Nxy28lQ3AIZtOql9bOTtCa1a66" crossorigin="anonymous">
    <style>
      /* Styling untuk navigasi */
     
    /* Mengatur gaya dasar untuk body */
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f9;
    }

    /* Gaya untuk navigasi */
    nav {
      background-color: #333;
      padding: 10px 0;
      text-align: center;
    }

    /* Gaya untuk list menu */
    nav ul {
      list-style-type: none;
      margin: 0;
      padding: 0;
    }

    /* Gaya untuk tiap item menu */
    nav ul li {
      display: inline-block;
      margin: 0 15px;
    }

    /* Gaya untuk tautan */
    nav ul li a {
      color: white;
      text-decoration: none;
      font-size: 18px;
      font-weight: bold;
      padding: 10px 20px;
      border-radius: 4px;
      transition: background-color 0.3s ease;
    }

    /* Efek saat hover */
    nav ul li a:hover {
      background-color: #007BFF;
      color: white;
    }

    /* Efek saat item aktif */
    nav ul li a:active {
      background-color: #0056b3;
    }

    /* Responsif: menata menu untuk tampilan mobile */
    @media (max-width: 768px) {
      nav ul li {
        display: block;
        margin: 10px 0;
      }
    }

    table {
    width: 50%;
    border-collapse: collapse;
    margin: 10px 0;
    font-size: 16px;
    text-align: center;
}

/* Gaya untuk header tabel */
table thead tr {
    background-color: #009879;
    color: #ffffff;
    text-align: center;
    font-weight: bold;
}

/* Gaya untuk baris tabel */
table th, table td {
    padding: 7px 10px;
    border: 1px solid #ddd;
}

/* Gaya untuk baris zebra */
table tbody tr:nth-child(even) {
    background-color: #f3f3f3;
}

/* Gaya hover untuk baris */
table tbody tr:hover {
    background-color: #f1f1f1;
    cursor: pointer;
}

/* Responsif untuk layar kecil */
@media screen and (max-width: 600px) {
    table {
        font-size: 14px;
    }
    table th, table td {
        padding: 5px;
    }
}
  </style>
</head>